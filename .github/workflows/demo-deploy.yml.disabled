name: GRC3 Product DEMO - Deploy (EC2 build + PM2 restart)

on:
  push:
    branches:
      - demo

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # ---------------------------------
      # 1. Checkout
      # ---------------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # ---------------------------------
      # Slack: Deployment Started
      # ---------------------------------
      - name: Slack - Deployment Started
        uses: slackapi/slack-github-action@v1.24.0
        with:
          payload: |
            {
              "text": "🚀 *GRC3 DEMO Frontend deployment started*\n• Branch: demo\n• Triggered by: ${{ github.actor }}\n• Building on DEMO EC2..."
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      # ---------------------------------
      # 2. Deploy + Build on DEMO EC2
      # ---------------------------------
      - name: Deploy & Build on DEMO EC2 (Frontend)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEMO_SERVER_HOST }}
          username: ${{ secrets.DEMO_SERVER_USER }}
          key: ${{ secrets.DEMO_SSH_KEY }}
          script: |
            set -e

            echo "🔄 Pulling latest frontend code..."
            cd /home/ubuntu/Dev_GRC/Frontend-GRC3
            git pull

            echo "🏗️ Building frontend on DEMO EC2..."
            npm run build

            echo "🔁 Restarting DEMO Frontend PM2..."
            pm2 restart next-app-full

      # ---------------------------------
      # Slack: Deployment Successful
      # ---------------------------------
      - name: Slack - Deployment Successful
        if: success()
        uses: slackapi/slack-github-action@v1.24.0
        with:
          payload: |
            {
              "text": "✅ *GRC3 DEMO Frontend deployment successful*\n• Branch: demo\n• Built & Deployed on DEMO EC2\n• PM2 restarted: next-app\n• Deployed by: ${{ github.actor }}"
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      # ---------------------------------
      # Slack: Deployment Failed
      # ---------------------------------
      - name: Slack - Deployment Failed
        if: failure()
        uses: slackapi/slack-github-action@v1.24.0
        with:
          payload: |
            {
              "text": "🔥 *GRC3 DEMO Frontend deployment FAILED*\n• Branch: demo\n• Triggered by: ${{ github.actor }}\n• Build/Deploy on EC2 FAILED\n• PM2 NOT restarted\n• Check logs urgently!"
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
